<Window x:Class="OrizontSetup.LanguageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Limba instalatorului"
        Width="480"
        Height="260"
        MinWidth="420"
        MinHeight="220"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="HeadingText"
                   Grid.Row="0"
                   FontSize="22"
                   FontWeight="SemiBold"
                   AutomationProperties.HeadingLevel="Level1" />
        <TextBlock x:Name="PromptText"
                   Grid.Row="1"
                   Margin="0,12,0,8"
                   AutomationProperties.Name="Instrucțiuni pentru alegerea limbii" />
        <ComboBox x:Name="LanguageComboBox"
                  Grid.Row="2"
                  MinHeight="32"
                  AutomationProperties.Name="Limba instalatorului"
                  SelectionChanged="LanguageComboBox_SelectionChanged" />
        <StackPanel Grid.Row="3"
                    Margin="0,20,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button x:Name="ContinueButton"
                    MinWidth="110"
                    Margin="0,0,10,0"
                    Padding="14,7"
                    IsDefault="True"
                    AutomationProperties.Name="Continuă cu limba aleasă"
                    Click="ContinueButton_Click" />
            <Button x:Name="CancelButton"
                    MinWidth="100"
                    Padding="14,7"
                    IsCancel="True"
                    AutomationProperties.Name="Anulează instalarea"
                    Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
