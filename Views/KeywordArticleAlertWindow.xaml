<Window x:Class="CititorRSS.Jaws.KeywordArticleAlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Articole găsite după cuvintele-cheie"
        Width="760" Height="480" MinWidth="560" MinHeight="340"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="CanResize"
        PreviewKeyDown="Window_PreviewKeyDown">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock x:Name="Heading" Grid.Row="0" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
    <TextBlock x:Name="Instructions" Grid.Row="1" Margin="0,8,0,12" TextWrapping="Wrap" />
    <ListBox x:Name="ResultsList" Grid.Row="2" DisplayMemberPath="AccessibleText"
             SelectionMode="Single" SelectionChanged="ResultsList_SelectionChanged"
             PreviewKeyDown="ResultsList_PreviewKeyDown"
             AutomationProperties.Name="Rezultatele cuvintelor-cheie, listă"
             AutomationProperties.HelpText="Folosește săgețile pentru a alege un articol. Enter îl deschide; Tab ajunge la acțiuni; Escape închide fereastra.">
      <ListBox.ItemContainerStyle>
        <Style TargetType="{x:Type ListBoxItem}">
          <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleText}" />
        </Style>
      </ListBox.ItemContainerStyle>
    </ListBox>
    <TextBlock x:Name="Feedback" Grid.Row="3" Margin="0,10,0,0" TextWrapping="Wrap"
               AutomationProperties.Name="Mesaj despre acțiunea pentru articol"
               AutomationProperties.LiveSetting="Polite" />
    <StackPanel x:Name="Buttons" Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button x:Name="OpenArticleButton" Content="Deschide articolul" MinWidth="150" Margin="0,0,8,0"
              IsDefault="True" Click="OpenArticle_Click" />
      <Button x:Name="ReadLaterButton" Content="Adaugă la Mai târziu" MinWidth="170" Margin="0,0,8,0"
              Click="ReadLater_Click" />
      <Button x:Name="CloseButton" Content="Închide" MinWidth="100" IsCancel="True" />
    </StackPanel>
  </Grid>
</Window>
